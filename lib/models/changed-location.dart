class ChangedLocation {
  num lat, lng;
  String googlePlaceFormattedAddress, googlePlaceArea;
  ChangedLocation(
      {this.lat,
      this.lng,
      this.googlePlaceFormattedAddress,
      this.googlePlaceArea});
}
